# Stride Adaptive — Environment Variables
# For Vite apps, all public env vars MUST start with VITE_
# This file is safe to commit because the key is the one you expose to your own browser.
# If you want to keep it private, add .env to .gitignore.

# Paste your Gemini API Key here (get one free at https://aistudio.google.com/app/apikey)
VITE_GEMINI_API_KEY=
